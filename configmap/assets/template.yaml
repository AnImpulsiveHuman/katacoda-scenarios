apiVersion: templates.gatekeeper.sh/v1beta1
kind: ConstraintTemplate
metadata:
  name: k8sdenyconfigmap
  annotations:
    description: Controls usage of host networking and ports.
spec:
  crd:
    spec:
      names:
        kind: K8sDenyConfigMap
      validation:
        # Schema for the `parameters` field
        openAPIV3Schema:
          properties:
            paths:
              type: array
              items:
                type: string
  targets:
    - target: admission.k8s.gatekeeper.sh
      rego: |
        package k8sdenyconfigmap

        violation[{"msg": msg, "details": {}}] {
         #input.review.object.kind == "Pod"                                #Checks if the kind is "Pod"
         #allowedconfigmaps := input.parameters.configmaps                 #Stores the allowed volumes from the parameter in a variable
         #configmap := input.review.object.spec.volumes[_].configMap.name  #Stores the name of the configmap volume in a variable
         #not contains(allowedconfigmaps,configmap)                        #Returns true if "contains()" returns false
         #msg := sprintf("%v - configmap not allowed",[configmap])         #Deny message
        }

       # contains(allowedconfigmaps,configmap) {                            
         #configmap == allowedconfigmaps[_]                                #Returns true if the volume name is not among the list of allowed names
       # }
